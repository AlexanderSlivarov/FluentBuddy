<?xml version="1.0" encoding="UTF-8" ?>
<Application
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="FluentBuddy.App">

    <Application.Resources>
        <ResourceDictionary>

            <Color x:Key="AppBackground">#101014</Color>
            <Color x:Key="SurfaceColor">#18181F</Color>
            <Color x:Key="SurfaceAltColor">#20202A</Color>
            <Color x:Key="BorderColor">#2C2C38</Color>

            <Color x:Key="PrimaryText">#FFFFFF</Color>
            <Color x:Key="SecondaryText">#B7B7C9</Color>
            <Color x:Key="MutedText">#8E8EA0</Color>

            <Color x:Key="AccentPurple">#9B6DFF</Color>
            <Color x:Key="AccentBlue">#5B8CFF</Color>
            <Color x:Key="AccentGreen">#35C98B</Color>
            <Color x:Key="AccentOrange">#FFB84D</Color>

            <Color x:Key="PrimaryButtonColor">#9B6DFF</Color>
            <Color x:Key="PrimaryButtonHoverColor">#AE88FF</Color>
            <Color x:Key="SecondaryButtonColor">#232330</Color>
            <Color x:Key="SecondaryButtonHoverColor">#2D2D3A</Color>

            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource AppBackground}" />
            </Style>

            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource PrimaryText}" />
            </Style>

            <Style TargetType="Border">
                <Setter Property="BackgroundColor" Value="{StaticResource SurfaceColor}" />
                <Setter Property="Stroke" Value="{StaticResource BorderColor}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="StrokeShape" Value="RoundRectangle 20" />
            </Style>

            <Style x:Key="AnimatedButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource SecondaryButtonColor}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="16" />
                <Setter Property="HeightRequest" Value="52" />
                <Setter Property="BorderColor" Value="{StaticResource BorderColor}" />
                <Setter Property="BorderWidth" Value="1" />
                <Setter Property="Padding" Value="18,12" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Shadow">
                    <Shadow
                        Brush="#000000"
                        Offset="0,4"
                        Radius="10"
                        Opacity="0.25" />
                </Setter>

                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup Name="CommonStates">
                            <VisualState Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="1" />
                                    <Setter Property="Opacity" Value="1" />
                                    <Setter Property="BackgroundColor" Value="{StaticResource SecondaryButtonColor}" />
                                </VisualState.Setters>
                            </VisualState>

                            <VisualState Name="PointerOver">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="1.03" />
                                    <Setter Property="BackgroundColor" Value="{StaticResource SecondaryButtonHoverColor}" />
                                </VisualState.Setters>
                            </VisualState>

                            <VisualState Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="0.98" />
                                    <Setter Property="Opacity" Value="0.9" />
                                </VisualState.Setters>
                            </VisualState>

                            <VisualState Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="Opacity" Value="0.5" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <Style x:Key="PrimaryAnimatedButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource AnimatedButtonStyle}">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryButtonColor}" />
                <Setter Property="BorderColor" Value="{StaticResource PrimaryButtonColor}" />

                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup Name="CommonStates">
                            <VisualState Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="1" />
                                    <Setter Property="Opacity" Value="1" />
                                    <Setter Property="BackgroundColor" Value="{StaticResource PrimaryButtonColor}" />
                                </VisualState.Setters>
                            </VisualState>

                            <VisualState Name="PointerOver">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="1.03" />
                                    <Setter Property="BackgroundColor" Value="{StaticResource PrimaryButtonHoverColor}" />
                                </VisualState.Setters>
                            </VisualState>

                            <VisualState Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="0.98" />
                                    <Setter Property="Opacity" Value="0.92" />
                                </VisualState.Setters>
                            </VisualState>

                            <VisualState Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="Opacity" Value="0.5" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>